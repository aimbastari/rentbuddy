import React from 'react';
import { Segment } from 'semantic-ui-react';

const Maintenance = ({}) => (
  <Segment>Maintenance Page</Segment>
);

Maintenance.propTypes = {
};

export default Maintenance;
